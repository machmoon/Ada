# Copy this file to .env and fill in your provider credentials.
# Mudrik itself does not read .env directly; these values are consumed by
# the OpenCode CLI when it runs against providers you've configured.

# Example: Ollama / remote-hosted model API key
OLLAMA_API_KEY=
