# Build Notes

## 2026-08-30

- Created `codex/pcb-placement-agent` in a clean worktree from `origin/main` at
  `60ff31b`; the dirty evidence-grounding checkout remains untouched.
- Narrowed the protected deliverable to verifiable PCB placement repair plus
  explicit company preferences.
- Kept deterministic legality separate from the Gemini policy, SFT data, and
  future RL backend.
