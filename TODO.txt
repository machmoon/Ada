TODO — agent layer (from DEVPOST.md "[not yet built]" section)

Decision (2026-08-30): James reversed the 2026-08-29 archive decision. ADK 2.8
dynamic workflows now drive the pipeline, tracked as feature 12 below, and
SILKSCREEN_ENGINE selects which driver runs. For the record, the idea had been
archived a day earlier because the SDK implementation already covered the agent
behaviour (analysis in docs/agent-framework.md); the reversal moves the
framework claim onto ADK itself rather than onto a wrapper around the SDK.

2026-08-30: the pipeline emits typed stage and model-call events through
generate_pcb(on_event=...) — an ADK-shaped seam mirroring ADK's callback/event
model. That is where the ADK workflow attached, exactly as designed.

RAG / retrieval is Leo's lane and is tracked separately — retrieval.py
(GeminiEmbedder, page-level citations) is the starting point; do not pick up
retrieval work from this file.

NOTE: DEVPOST's agent section is being rewritten in ADK 2.0 vocabulary as part
of the adoption — dynamic workflows and nodes, not the template agents of the
1.x topology it described as the plan.

[EXAMPLES OF PROMPTS]
These examples stay inside the emitter's current footprint set: SOT-223,
SOIC, LQFP, and two-terminal chip passives. Until connector footprints are
implemented, keep connectors, headers, switches, buttons, and test points out
of demo requests.

- A minimal 3.3 V LDO regulator using an AMS1117-3.3 in SOT-223, with a
  10 uF input capacitor from VIN to GND and a 22 uF output capacitor from
  +3V3 to GND. Use only the regulator and passive components; do not add
  connectors, headers, switches, buttons, or test points.
- A roughly 1 Hz LED blinker using an NE555D in SOIC-8 on a +9 V rail, with
  astable timing resistors and capacitor, 100 nF supply decoupling, and one
  LED with a series current-limiting resistor. Use only the SOIC IC and
  two-terminal passives; do not add connectors, headers, or switches.
- A minimal ATtiny85-20SU board in SOIC-8 on a +5 V rail, with a 100 nF VCC
  decoupling capacitor, a 10 kOhm reset pull-up, and a status LED with a
  series resistor on PB1. Do not add an ISP header, connectors, buttons, or
  test points.
- A buffered 2.5 V reference on a +5 V rail using an LM358D in SOIC-8: two
  10 kOhm resistors form the divider, one amplifier is a voltage follower,
  the unused amplifier is tied to a defined state, and the supply has a
  100 nF decoupling capacitor. Do not add connectors or test points.
- Reviewer demo: an AMS1117-3.3 regulator in SOT-223 with a 10 uF input
  capacitor and a 22 uF ceramic output capacitor. Use only the regulator and
  passive components, and preserve the ceramic output-capacitor choice so the
  adversarial review can check it against the selected AMS1117 datasheet.

FEATURES
--------
[DONE-SDK] means shipped on the plain SDK path; the ADK re-expression of that
feature is tracked under feature 12.

 #   Feature / sub-feature                             Status        Note
 --  ------------------------------------------------  ------------  --------------------------------------------
 1   Pipeline sequence                                 [DONE-SDK]    now: ADK Workflow node
 ----- Read/propose+validate/place/review sequence     [DONE]        validation is embedded in propose.py
 ----- Model protocol seam; stages never see the SDK   [DONE]        model.py; ScriptedModel keeps tests offline
 ----- ADK SequentialAgent re-expression               [SUPERSEDED]  ADK 2.0 dynamic workflows took its place; the
                                                                     template class still ships, see feature 12
 2   Parallel datasheet reads                          [NOT BUILT]   now: the R11 rung under feature 12
 ----- asyncio.gather fan-out, one read per part       [NOT BUILT]   inside the ADK read node; see 12 R11
 ----- Client concurrency decided and tested           [NOT BUILT]   shared genai client vs one per task
 ----- Failure isolation + concurrency cap             [NOT BUILT]   one bad part must not sink the batch
 3   Validation repair loop                            [DONE-SDK]    now: for-loop inside an ADK node
 ----- Batched error collection, all failures at once  [DONE]        parse_circuit_spec, netlist.py
 ----- Bounded loop, ends when the IR validates        [DONE]        propose.py
 ----- A pin on two nets rejected at validation        [DONE]        landed in PR #10, which also edits
                                                                     netlist.py. Two nets sharing a pin are
                                                                     electrically one and every consumer resolved
                                                                     it differently. Batched with the rule below
                                                                     so the repair loop corrects the model.
 ----- Two pin names on one pin number rejected        [DONE]        Greptile P1 on PR #10: the number is what
                                                                     reaches the footprint and the symbol, so the
                                                                     second name overwrote the first and one
                                                                     specified connection vanished from both
                                                                     emitted files without raising.
 ----- ADK LoopAgent wrapper                           [SUPERSEDED]  the bounded loop is plain Python inside a
                                                                     node under ADK 2.0; see feature 12
 4   Adversarial reviewer                              [DONE-SDK]
 ----- Refute-not-confirm prompt                       [DONE]        review.py
 ----- Findings filtered against the actual spec       [PARTIAL]     unknown part refs dropped; findings retained
 ----- Specialised reviewer split, merged verdict      [IDEA]        power / signal integrity / manufacturability
 5   Model tiering                                     [DONE-SDK]
 ----- FallbackModel chain w/ response validation      [DONE]        resilience.py
 ----- 3.7-flash primary, 3.5-flash-lite second        [DONE]        service/app.py
 ----- Per-task routing beyond failover                [IDEA]        vision/reasoning vs mechanical passes
 ----- Request timeout so a hung primary fails over    [DONE]        60 s http_options deadline (SDK unit: ms) on
                                                                     GeminiModel + GeminiEmbedder clients; override
                                                                     SILKSCREEN_MODEL_TIMEOUT_S; timeout -> ModelError
 ----- Gemma open-weights last-resort rung             [DONE]        gemma-4-31b-it as the third build_model
                                                                     provider, one attempt; a different model family
                                                                     is unlikely to share the Gemini tiers' outage
                                                                     or quota pool. Key-gated live test exercises
                                                                     the rung for real
 6   Confirmation gates on file writes                 [NOT BUILT]
 ----- Inventory write points, define the gate         [NOT BUILT]   save_board, CLI output, service response
 ----- CLI confirmation prompt before board write      [NOT BUILT]   with a --yes bypass
 ----- Service dry-run mode                            [NOT BUILT]   placement report without writing a board
 7   Cloud Run deployment                              [PARTIAL]
 ----- HTTP surface + Dockerfile, tested offline       [DONE]
 ----- The deploy itself, live URL in README           [DONE]        2026-08-31: gcloud run deploy --source from a
                                                                     clean main clone; project kaleo-hack-2026,
                                                                     us-central1; key via Secret Manager, Firestore
                                                                     db created, GOOGLE_CLOUD_PROJECT set
 ----- Post-deploy smoke check                         [DONE]        /, /models and one real /generate verified
                                                                     live 2026-08-31: the AMS1117 demo prompt came
                                                                     back optimal, 5 parts, 3/3 nets routed, in
                                                                     10.5 s. NOTE: Google's frontend intercepts
                                                                     /healthz on run.app at the edge (404 before
                                                                     the container); /readyz is the alias external
                                                                     probes must use
 8   Firestore fact cache                              [DONE]
 ----- FactStore protocol + MemoryFactStore            [DONE]        service/cache.py; tests need no GCP project
 ----- Real write-back + preloaded_facts feed-in       [DONE]        commit 4249c5b
 ----- Ops polish: client reuse, document env var      [OPEN]        USE_FIRESTORE undocumented today
 9   Web review UI                                     [PARTIAL]
 ----- Review page, served by the service at /         [DONE]        frontend/ Svelte SPA; same origin, so no CORS
 ----- Schematic topology contract + SVG viewer        [DONE]        generic symbols; net-labelled validated pins
 ----- Board well (Option B), placements in the API    [DONE]        service resolves the geometry; SVG flips Y once
 ----- Findings cross-highlight across all views       [DONE]        review, schematic and board share selection
 ----- Grounding toggle on the intent form             [DONE]        sends "ground" only when a datasheet is given
 ----- Download the emitted .kicad_pcb                 [DONE]        from the board well and the review results
 ----- In-app debug console + log export               [DONE]        console/window capture; text + NDJSON
 ----- Persistent chat transcript + artifact cards    [DONE]        friendly activity; raw prompts/responses expand
 ----- Save/open versioned JSON sessions              [DONE]        transcript, trace, result and board artifact
 ----- Retry/edit/switch-model recovery controls      [DONE]        failures stay in the transcript for comparison
 ----- data-testid sweep across every component        [DONE]        stable hooks for e2e and agent QA
 ----- Voice: spoken intent, findings read aloud       [NOT BUILT]   the last phase, by instruction; Whisper
 10  Service error taxonomy                            [OPEN]
 ----- Split request errors from internal ValueErrors  [OPEN]        PR #5 review: an internal ValueError from board
                                                                     generation/packing surfaces as a 400 with its raw
                                                                     message instead of the sanitized 500 + error_id.
                                                                     Fix is a dedicated RequestError type raised only
                                                                     for field validation; do_POST catches that alone
                                                                     and lets ValueError fall to the 500 path.
                                                                     The fix must cover /generate/stream's run.error
                                                                     frames too — both routes share _error_response.

 11  Live pipeline events                              [DONE]
 ----- Engine on_event callback, stage+model events   [DONE]        pipeline.py; the ADK-shaped seam (note above)
 ----- POST /generate/stream, NDJSON frames           [DONE]        service/app.py; run.done carries the one-shot body
 ----- Chat feed + live stage ticks + console mirror  [DONE]        PipelineFeed.svelte; src 'server' log entries
 ----- Raw model responses behind a "debug" flag      [DONE]        model.response frames, clipped at 16 kB
 ----- Raw model requests + correlated call ids       [DONE]        system/user prompts, documents and settings
 ----- POST /chat/stream, versioned agent events       [DONE]        one clarification or a terminal board result
 ----- Fallback must never rerun a started run        [OPEN]        PR #9 review (P2, genuine): generateStream falls
                                                                     back to one-shot on a 200 whose content type was
                                                                     rewritten or whose body is unreadable — but a 200
                                                                     means the service already started a paid run, so
                                                                     the fallback duplicates model calls. Tighten the
                                                                     fallback to the 404 branch only.

 12  Google ADK adoption (dynamic workflows)           [PARTIAL]     ADK 2.8 owns orchestration; SILKSCREEN_ENGINE
                                                                     selects the driver
 ----- R1 install + offline smoke                      [DONE]        google-adk 2.8 imports and runs with no key
 ----- R2 spike: workflow, state, exceptions           [DONE]        throwaway, in the scratchpad, never committed
 ----- R3 dependency + pin                             [DONE]        adk extra; google-genai pinned >=2.19,<3
 ----- R4 stages.py shared stage bodies                [DONE]        one body per stage, both drivers call it
 ----- R5 @node graph in adk/workflow.py               [DONE]        read, propose, place and review as nodes
 ----- R6 in-process runner in adk/runner.py           [DONE]        Runner and session per run; token registry
 ----- R7 engine dispatcher, default "sdk"             [DONE]        kwarg > SILKSCREEN_ENGINE > default
 ----- R8 parity suite, test_adk.py                    [DONE]        same events and same result on both engines
 ----- R12 Dockerfile + setup gain the adk extra       [DONE]        image, CI and setup all install the extra
 ----- R10 default engine flips to adk                 [DONE]        gate passed 2026-08-30: live SPA run,
                                                                     optimal placement, failover observed
 ----- LLM root orchestrator + generate_board tool     [DONE]        ADK LlmAgent decides clarification vs tool call
 ----- Gemini models.list discovery + Auto policy     [DONE]        /models; server-filtered retry choices
 ----- R9 agents dir for adk web and /docs             [NOT BUILT]   a dev surface only, off the request path
 ----- R11 parallel reads via asyncio.gather           [NOT BUILT]   closes feature 2; needs an order-independent
                                                                     event test
 ----- R13 ScriptedLlm(BaseLlm) adapter                [NOT BUILT]   the offline stand-in for an LlmAgent path
 ----- R14 Pydantic transport schemas                  [NOT BUILT]   CircuitSpec stays a dataclass; the batched
                                                                     ValidationError must survive
 ----- R15 review as an LlmAgent                       [NOT BUILT]   the first stage to leave the Model protocol
 ----- R16 FallbackLlm vs RetryConfig                  [NOT BUILT]   served_by and model.retry must survive
 ----- R17 validate as a real stage                    [IDEA]        today it lives inside propose
 ----- R18 retire _EventingModel and the SDK path      [IDEA]        only once R10 has held

 13  ADK reversal: docs and decision record            [PARTIAL]
 ----- D0 CLAUDE.md ADK decision reversed              [DONE]        the reversal and its three decisions
 ----- D1 this file: decision, notes, features         [DONE]        12 and 13 added, ADK rows reworded
 ----- D2 docs/agent-framework.md rewritten            [DONE]        the framework claim now rests on ADK
 ----- D3 DEVPOST agent section in ADK vocabulary      [DONE]        dynamic workflows, not template agents
 ----- D4 CLAUDE.md seam, setup, framework claim       [DONE]        layout, dispatcher, extras, known issue 6
 ----- D5 check_docs --fix once counts settle          [OPEN]        run after the ADK tests land

 14  Schematic emission (.kicad_sch + .kicad_pro)     [DONE]        PR #10; a run leaves a KiCad project, not
                                                                     one board file
 ----- Generated symbols, embedded lib_symbols         [DONE]        opens where no KiCad library is installed
 ----- Net-label stubs, one per validated pin          [DONE]        pin-level, matching netlist.py's contract
 ----- Shared refs so C3 is C3 in both files           [DONE]        CircuitSpec.assign_refs; both emitters call it
 ----- Verified by KiCad's own ERC                     [DONE]        kicad-cli 8.0.9: 0 errors. Found and fixed
                                                                     four geometry bugs first, incl. every
                                                                     passive's net label drawn on its own body
 ----- Sheet layout beyond one A4 page                 [NOT BUILT]   overflow is reported, not paginated
 ----- Surface the schematic in the web UI             [OPEN]        the stage only runs with an output path and
                                                                     the service passes none, so the SPA has no
                                                                     schematic to show and deliberately no rail
                                                                     row for it. Needs a service-side decision
                                                                     about returning the sheet, like kicad_pcb.
 15  Copper routing (two-layer grid maze router)       [PARTIAL]     PR #10
 ----- A* over a 0.25 mm lattice, explicit via cost    [DONE]        routing.py
 ----- Unroutable nets named, never silently dropped   [DONE]        incl. both refusal paths, so a skipped route
                                                                     cannot report 100% complete
 ----- Clearance enforced inside the search            [DONE]        was left to a halo that could not claim nodes
                                                                     another net owned; KiCad DRC found the
                                                                     resulting short. 14 DRC errors -> 2, and both
                                                                     survivors are the footprint's own geometry
 ----- Bounded search, in expansions not seconds       [DONE]        400k total / 150k per net, sized from
                                                                     measurement; worst case 76s -> under 7s
 ----- Rip-up and retry                                [NOT BUILT]   sequential and order-dependent, so it paints
                                                                     itself into corners a real router escapes
 ----- Fine-pitch packages                             [NOT BUILT]   the 0.25 mm grid cannot reach every pin of an
                                                                     LQFP; measured 6/50 nets on a dense board.
                                                                     Needs a finer grid or off-grid pad escapes
 ----- Route the copper in the web UI's board well     [NOT BUILT]   the downloaded .kicad_pcb has tracks; the SVG
                                                                     board well still draws placement only
 ----- DRC in CI                                       [IDEA]        kicad-cli is a local gate today; no KiCad on
                                                                     the runners and installing one would dominate
                                                                     the job

 16  SPICE simulation as a verifier                    [PARTIAL]     passive-network verification is real; trusted
                                                                     IC model sourcing and agent-loop use remain
 ----- CircuitSpec -> SPICE netlist                   [DONE]        spice/deck.py; the caller supplies sources,
                                                                     analysis and trusted behavioural models
 ----- Declarative measurements + assertions          [DONE]        measured value and signed margin per clause
 ----- ngspice runner, verified end to end            [DONE]        CI checks RC results against closed-form theory
 ----- LTspice runner                                 [UNVERIFIED]  process launch has not run against LTspice;
                                                                     real and complex binary RAW have offline fixtures
 ----- MCP simulate_circuit + spice_capabilities      [DONE]        mcp/server.py; spice/spec.py is the JSON bridge
 ----- Secure executable-deck boundary                [DONE]        typed MCP input only; directives rejected before
                                                                     launch; runtime/output controls are bounded
 ----- Windowed reference bandwidth alignment         [DONE]        numerator and reference use identical windowed
                                                                     frequency samples; zero reference fails loudly
 ----- Device models for ICs                          [OPEN]        use a trusted server-side model registry; never
                                                                     execute raw model programs supplied over MCP
 ----- Wire simulation into the agent loop            [OPEN]        future propose -> simulate -> repair feedback

 17  Verifiable placement repair                       [PARTIAL]
 ----- Portable geometry and backend-neutral reward   [DONE]        silkscreen/placement/
 ----- Company hard rules and soft preferences        [DONE]        compact-control + thermal-first
 ----- Generated-board adapter                        [DONE]        verifier runs before routing; write-back remains
                                                                     integer nanometres and may expand a tight outline
 ----- Gemini and deterministic action policies       [DONE]        bounded verifier-gated loop
 ----- Experimental Ollama/Tinker policies            [DONE]        explicit default-off UI + server environment gate
 ----- Engineer correction memory                     [PARTIAL]     request/session-local lab only; tenant durability
                                                                     still requires authentication and ownership
 ----- Integrated placement view and JSON download    [DONE]        generated boards, receipts and session export
 ----- Synthetic behavior-cloning trajectory export  [DONE]        no trained checkpoint claimed

 18  Slack integration                                 [DONE, unverified live]
 ----- Signed events + slash commands, threaded runs  [DONE]        slackbot/app.py; HMAC before parse, event_id dedup
 ----- design / place / review verbs over the pipeline[DONE]        slackbot/runner.py; calls generate_pcb unchanged
 ----- Board preview + .kicad_pcb into the thread     [DONE]        slackbot/render.py; SVG always, PNG with Pillow
 ----- Prepared fabrication order, never submitted    [DONE]        slackbot/order.py. DELIBERATE BOUNDARY: the bot
                                                                     prepares an order and stops. No vendor call, no
                                                                     submission, no payment path exists, and
                                                                     test_order.py enforces it by import (only
                                                                     slack.py may import a network module). A human
                                                                     confirms before money is spent. If ordering is
                                                                     ever taken further, that gate is the design, not
                                                                     an obstacle to route around.
 ----- Test against a real Slack workspace            [OPEN]        Built against the API and never run against a
                                                                     live workspace: no app was created and no token
                                                                     requested. Request construction is covered by
                                                                     tests against a recorded transport, but a scope
                                                                     or payload detail could still be wrong on the
                                                                     first real run.
 ----- Promote the placements geometry serializer     [OPEN]        slackbot/render.py imports the private
                                                                     service.app._placements_dict rather than
                                                                     duplicating the rotation and pad-offset maths.
                                                                     Duplicating it is the board-destroying bug class,
                                                                     so importing was right, but it wants to be public
                                                                     on the engine or a shared module. Left alone
                                                                     because another session was in the engine.
 ----- Persist run records across a restart           [OPEN]        Runs are remembered per thread in memory, so
                                                                     review/order forget after a restart (the bot says
                                                                     so rather than acting on the wrong board). A
                                                                     second replica would need a real store.

 19  Ordering: fab package, gate, quote, human stop     [PARTIAL]
 ----- Fab package: Gerber, drill, BOM, CPL            [DONE]        fab.py
 ----- Drill split by plating + fab notes README       [DONE]        PTH/NPTH always ship; README-fab.txt answers
                                                                     the questions a CAM engineer would email
 ----- Pre-flight gate: 12 checks with evidence        [DONE]        gate.py; SKIPPED blocks like FAIL
 ----- Real fab capability profiles + a real quote     [DONE]        fabhouse.py; OSH Park priced from its
                                                                     published rule, JLCPCB/PCBWay unpriced
 ----- Prepared order + silkscreen-order CLI           [DONE]        approval.py; exit 0 GO, 2 NO-GO
 ----- Gate and quote on /generate's order block       [DONE]        additive; no route submits
 ----- Order tab in the web UI                         [NOT BUILT]   the service already returns gate, quote and
                                                                     the rendered summary; this is a view over
                                                                     data that already ships
 ----- BOM part numbers (LCSC/MPN resolution)          [NOT BUILT]   the gate blocks assembly without them, which
                                                                     is the honest state until a resolver exists.
                                                                     Needs a supplier lookup, so it is agent-lane
                                                                     work, not engine-lane
 ----- Wider via annular ring so JLCPCB will build it  [OPEN]        the router's 0.6/0.3 mm vias leave 0.15 mm,
                                                                     under JLCPCB's stated 0.18 mm absolute
                                                                     minimum. Clears OSH Park. Router-lane fix
 ----- Order submission                                [WILL NOT BUILD]
                                                                     Deliberate and permanent. See
                                                                     fabhouse.SUBMISSION_BOUNDARY: the gate
                                                                     proves what it measures, not that the
                                                                     circuit is the one the buyer meant, and an
                                                                     agent that clears its own checks and then
                                                                     buys converts every remaining design error
                                                                     into money at machine speed. Do not add a
                                                                     submit path, a --yes flag, or a fab's
                                                                     authenticated API.

OPEN WORK, SUMMARISED
---------------------
Everything actionable reduces to: feature 2 in full (parallel reads, now the
R11 rung under 12), finding-level hallucination filtering under 4, the request
timeout under 5, feature 6 in full (confirmation gates), the deploy and smoke
check under 7, the ops polish under 8, voice under 9, the error taxonomy under
10 (from PR #5 review), and the stream-fallback tightening under 11 (from PR #9
review), and the three open rows under 18 (Slack: live-workspace test,
serializer promotion, run persistence). The board well shipped, so voice is
all that is left of 9 and stays last. Feature 16 still needs a trusted IC
model registry and its place in the agent repair loop. The [IDEA] rows under
4 and 5 are ideas, not commitments.

The ordering lane (feature 14) is complete up to the human stop, which is
where it is meant to end. What is left under it is a web view over data the
service already returns, BOM part-number resolution (agent-lane), and a
router-lane via change; submission is marked WILL NOT BUILD rather than
NOT BUILT, and that row is a decision, not a backlog item.

The ADK lane runs alongside that. Feature 12 is live and the flip is done: the
default engine has been "adk" since the 2026-08-30 gate run, with
SILKSCREEN_ENGINE=sdk as the kill switch, and the image, CI and setup all
install the adk extra. What remains under 12 is the queue behind the flip —
R9 and R11, then R13 through R16, with R17 and R18 as ideas. Feature 13 is
complete, D5 included (check_docs --fix ran once the counts settled).

Features 14 and 15 landed with PR #10 and changed what a run produces: a whole
KiCad project rather than one placed board file. What is left of them is the
web UI catching up (the SPA shows neither the schematic nor the tracks) and the
router's two honest limits, rip-up-and-retry and fine-pitch packages.

Worth knowing for anything touching the emitters: KiCad 8.0.9 is installed
locally and `kicad-cli sch erc` / `pcb drc` are now the strongest check the
project has. The first DRC ever run on an emitted board found a real short that
the whole test suite passed over. See "Verifying against real KiCad" in
CLAUDE.md. It is a local gate, not a CI one.
