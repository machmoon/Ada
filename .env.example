# Copy to .env and fill in. Never commit .env.
GOOGLE_API_KEY=
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=us-central1
# Root of the built web bundle; defaults to <repo>/web/dist, /app/web/dist in the container.
SILKSCREEN_WEB_DIST=
